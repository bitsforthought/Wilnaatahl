import React from "react";

/** The bare "×" that dismisses a toast or an overlay. */
export const dismissButtonStyle: React.CSSProperties = {
  background: "transparent",
  border: "none",
  color: "inherit",
  fontSize: "1.6em",
  lineHeight: 1,
  padding: "0 0.25em",
  cursor: "pointer",
};
